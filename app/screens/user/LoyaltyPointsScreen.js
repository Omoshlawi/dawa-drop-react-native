import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LoyaltyPointsScreen = ({ navigation }) => {
  return (
    <View>
      <Text>LoyaltyPointsScreen</Text>
    </View>
  );
};

export default LoyaltyPointsScreen;

const styles = StyleSheet.create({});
