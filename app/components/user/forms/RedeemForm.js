import { StyleSheet, Text, View } from "react-native";
import React from "react";

const RedeemForm = ({ navigation, route }) => {
  const data = route.params;
  return (
    <View>
      <Text>RedeemForm</Text>
    </View>
  );
};

export default RedeemForm;

const styles = StyleSheet.create({});
